import styled from "styled-components";

export const Container = styled.footer`
  position: fixed;
  bottom: 0;
  width: 100vw;
  padding: 1rem;
  color: var(--primary);
  font-weight: bold;
  text-align: center;
  background-color: var(--bg);
  box-shadow: 0 -1rem 3rem 0 rgba(0, 0, 0, .25);
`;

export const Text = styled.p``;
