import styled from "styled-components";

export const Container = styled.footer`
  position: fixed;
  bottom: 0;
  width: 100vw;
  padding: 1rem;
  color: var(--primary);
  font-weight: bold;
  text-align: center;
`;

export const Text = styled.p``;
